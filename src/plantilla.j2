<!DOCTYPE html>
<html>
<head>
    <title>Bingo Web</title>
    <meta charset="UTF-8">
</head>

<style>
body {
    margin: 0;
    padding: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    height: 100vh;
}

.tabla {
    margin: 2em;
    display: flex;
    flex-direction: column;
}

.fila {
    display: flex;
}

.fila > div {
    border: 1px solid #001f3f;
    width: 4em;
    height: 4em;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 2em;
    text-align: center;
}

.celda {
    background: #CE4C45;
    color: black;
}

.vacia {
    background: #C1A04F;
}
</style>

<body>
    <div class="tabla">
        {% for fila in carton %}
        <div class="fila">
            {% for celda in fila %}
            {% if celda == 0 %}
            <div class="vacia">
            </div>
            {% else %}
            <div class="celda">
                {{ celda }}
            </div>
            {% endif %}
            {% endfor %}
        </div>
        {% endfor %}
    </div>
</body>
</html>
